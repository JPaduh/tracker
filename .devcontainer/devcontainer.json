{
  "name": "job-tracker-dev",
  "build": { "dockerfile": "Dockerfile" },
  "forwardPorts": [8000, 5173],
  "portsAttributes": {
    "8000": { "label": "FastAPI Backend" },
    "5173": { "label": "React Frontend (Vite)" }
  },
  "postCreateCommand": "python -m pip install --upgrade pip && pip install -r backend/requirements.txt && if [ -f frontend/package.json ]; then cd frontend && npm install; fi && if [ -f package.json ]; then npm install; fi",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint"
      ]
    }
  }
}